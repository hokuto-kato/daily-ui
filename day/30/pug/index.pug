- contentName = "Daily UI - 30"
doctype html
html(lang="ja")
	head
		meta(charset="UTF-8")
		meta(name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0")
		meta(name="description" content=contentName)
		meta(http-equiv="X-UA-Compatible" content="ie=edge")
		title !{contentName}
	body
		main.main
			.container
				h1.main__title Daily UI 30
				.main__content
					p.main__body はじめてご利用の方
					button.main__button(type="button" data-trigger-dialog) 新規登録
					p.main__body アカウントをすでにお持ちの方
					button.main__button(type="button") ログイン
			.overlay.visually-hidden(data-overlay data-close)
			.dialog.visually-hidden(data-target-dialog)
				.dialog__close(data-close)
				h1.dialog__title 新規登録
				.dialog__input-wrap
					input.dialog__input(id="name" type="text" name="registration" data-input)
					label.dialog__input-label(for="name" data-placeholder) 名前
				.dialog__input-wrap
					input.dialog__input(id="mail" type="text" name="registration" data-input)
					label.dialog__input-label(for="mail" data-placeholder) メールアドレス
				button.dialog__button(type="button") 新規登録