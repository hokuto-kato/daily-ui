-
	contentName = "Daily UI - 10"

doctype html
html(lang="ja")
	head
		meta(charset="UTF-8")
		meta(name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0")
		meta(name="description" content=contentName)
		meta(http-equiv="X-UA-Compatible" content="ie=edge")
		title !{contentName}
	body
		.main
			header.header.visually-hidden(data-header)
				nav.nav
					ul.nav__box
						li.nav__item
							a.nav__item-body(href="https://example.com")
								h1.nav__item-heading Daily UI 10
						li.nav__item
								a.nav__item-body(href="https://example.com") メッセージ
						li.nav__item
								a.nav__item-body(href="https://example.com") 会社概要
						li.nav__item
								a.nav__item-body(href="https://example.com") 先輩社員の声
						li.nav__item
								a.nav__item-body(href="https://example.com") ある日の仕事内容
						li.nav__item
								a.nav__item-body.nav__item-body--entry(href="https://example.com") エントリー
			img.header__title.visually-hidden(src=require("~/img/title.svg"), alt="Daily UI 10", data-title)
		main.content
			h1.content__heading メッセージ
			.content__wrap
				-for (let i = 0; i < 10; i++)
					p.content__body
						-for (let i = 0; i < 20; i++)
							| テキストが入ります。